import {render,screen} from '@testing-library/react'; import {MemoryRouter} from 'react-router-dom'; import {describe,it,expect} from 'vitest'; import './i18n'; import App from './App';
describe('AJO site',()=>{it('renders Polish home and calculator',()=>{render(<MemoryRouter initialEntries={['/pl']}><App/></MemoryRouter>);expect(screen.getByRole('heading',{name:/Zobacz różnicę/})).toBeInTheDocument();expect(screen.getByText('Szacowana suma')).toBeInTheDocument()});it('renders privacy route',()=>{render(<MemoryRouter initialEntries={['/en/privacy']}><App/></MemoryRouter>);expect(screen.getByRole('heading',{name:'Privacy policy'})).toBeInTheDocument()})});
